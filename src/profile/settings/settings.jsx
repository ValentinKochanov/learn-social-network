import React from 'react';
import s from './set.module.css';

const Settings = () => {
    return (
        <div className={s.settings}>
            Settings
        </div>
    )
}

export default Settings;